package bemobile.splanes.com.gnbapp.commons.ui.base

import androidx.lifecycle.ViewModel

/**
 * Abstract class that can provide, in future, methods, utils or set common behavior (it should be
 * done with an interface also) for all ViewModels that extends from it.
 */
abstract class BaseViewModel : ViewModel() {
    // Now is empty
}