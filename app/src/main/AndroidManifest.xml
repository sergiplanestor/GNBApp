<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bemobile.splanes.com.gnbapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.GNBApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:fullBackupContent="@null"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true">
        <!-- Note: in case of API > 23 do:
                android:networkSecurityConfig="@xml/network_security_config"
             File is already created.
        -->
        <activity android:name=".feature.ui.MainActivity" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.ui.TransactionActivity"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>